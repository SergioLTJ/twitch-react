import { Grid } from '@material-ui/core';
import styled from 'styled-components';

export const ChannelLogo = styled.img`
  width: 100px;
  height: 100px;
  border: 1px solid black;
`;

export const ChannelGridItem = styled(Grid)``;
